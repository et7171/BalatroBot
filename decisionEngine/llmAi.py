"""
Uses a language model (e.g. ChatGPT or LM Studio) to generate decisions.
Takes game state as JSON and outputs action commands.
"""


class LlmAi:
    def chooseAction(self, state):
        # Send prompt to LLM and parse result
        return []
